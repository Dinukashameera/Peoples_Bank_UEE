import 'package:flutter/material.dart';

class InterbankTransferScreen extends StatelessWidget {
  static const routeName = '/interBankTransfer';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Inter bank Transfer Screen')
    );
  }
}
